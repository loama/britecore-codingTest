language: node_js
node_js:
  - "node"
install:
  - npm install -g firebase-tools
  - npm install
  - npm run build
  - npm run unit
script:
  -
deploy:
  provider: firebase
  skip_cleanup: true
  token:
    secure: $PRODUCTION_FIREBASE_TOKEN
  project: "britecore-codingtest"  
